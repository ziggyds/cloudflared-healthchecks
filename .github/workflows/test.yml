name: Tweet test
on:
  # Manueel starte
  workflow_dispatch:

jobs:
  X:
    name: Tweeting
    runs-on: ubuntu-latest
    steps:
      - uses: dart-actions/tweet@v1.0.0
        with:
          text: |
            Hi, this is a test!
          bearer-token: ${{ secrets.X_BEARER_TOKEN }}